<h1>All Contacts</h1>
<%= link_to "Add a contact", new_contact_path %>

<ul>
  <% @contacts.each do |contact| %>
    <li><%= link_to contact.name, new_message_path %>: <%= contact.phone %></li>
<% end %>
</ul>

<br>
<%= link_to "Add a message", new_message_path %>
